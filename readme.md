# 🧠 Natural Language to SQL Query Generator

## 📌 Project Overview

This project converts user natural language questions into SQL queries and executes them on a connected database.

Example:
User Input:
> Show all employees with salary greater than 50000

Generated SQL:
```sql
SELECT * FROM employees WHERE salary > 50000;